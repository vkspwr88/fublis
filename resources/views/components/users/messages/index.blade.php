{{-- <div class="row">
	<div class="col-md-7">
		<h5 class="text-black fs-5 fw-semibold mb-5">Your Messages</h5>
		<ChatSubjects :chat-subjects="chatSubjects" :selected-chat="{{ $selectedChat }}" :subjects-route="{{ route('architect.account.profile.message.subject') }}" />
	</div>
	<div class="col-md-5">
		<div class="row g-3">
			<div class="col-12">
				<ChatMessages :chat-messages="chatMessages" :chats-route="chatsRoute" />
			</div>
			<ChatForm :send-message-route="sendMessageRoute" />
		</div>
	</div>
</div> --}}

